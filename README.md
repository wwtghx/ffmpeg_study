# ffmpeg_study


1. Generate SSH, and save in github
ssh-keygen -t rsa -C "wwtg_hx@163.com"

2. clone ffmpeg-study repository
git clone git@github.com:wwtghx/ffmpeg_study.git

3. save changes
git add .
git commit -a -m "init"

4. push to server
git push origin master